import React from 'react';

const Details = ({detail}) => {
        console.log(detail)
        return (
                <div>
                        
                </div>
        );
};

export default Details;